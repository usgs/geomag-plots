'use strict';

var config = {
  src: 'src',
  build: '.build',
  buildPort: 8000,
  dataPort: 8007,
  dist: 'dist',
  distPort: 8003,
  etc: 'etc',
  example: 'example',
  examplePort: 8004,
  liveReloadPort: 8009,
  templatePort: 8008,
  test: 'test',
  testPort: 8001
};

module.exports = config;
